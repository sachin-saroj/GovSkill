# GovSkill Frontend — Environment Variables Example
# In development, Vite automatically proxies /api to http://localhost:8000 (see vite.config.ts).
# For production builds with a custom backend host, configure the API baseURL if required.

VITE_API_URL=/api
